震荡策略4H周期线
第一步：4H判断震荡
score = 0
if ADX < 20: score += 1
if EMA21 和 EMA55 很接近: score += 1
if ATR 下降: score += 1
if score >= 2:
    判定 = 震荡
else:
    判定 = 非震荡
第二步：1H 找区间（关键！）
在 1H 做：
方法1：布林带
上轨 = 压力
下轨 = 支撑

第三步：15M 入场（核心执行）
入场必须用小周期，否则震荡会亏钱

第四步：具体入场策略：
做多（低吸)条件：
1. 4H = 震荡
2. 价格接近 1H 下轨（支撑）
3. 15M 出现信号：
    - RSI < 30（超卖）
    - 或 Boll 下轨反弹
    - 或出现 Pin Bar / 吞没
入场：做多
止损：区间下方
止盈：区间中轨 or 上轨

做空（高抛)条件：
1. 4H = 震荡
2. 价格接近 1H 上轨（压力）
3. 15M 出现信号：
    - RSI > 70（超买）
    - 或 Boll 上轨回落
    - 或反转K线
入场：做空
止损：区间上方
止盈：区间中轨 or 下轨

假突破（插针）：
1. 4H = 震荡
如果价格突破区间，但 ADX 仍然 < 20
👉 判定为假突破 → 反向做
